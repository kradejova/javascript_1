const title = "Laska a jine zavislosti"

document.body.innerHTML += `
    <p>${title.length}</p>
    <p>${title.toUpperCase()}</p>
    <p>${title.slice(0,5)}</p>
    <p>${title.slice(title.length - 5, title.length)}</p>
`